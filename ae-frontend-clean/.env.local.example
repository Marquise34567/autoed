# Copy this file to .env.local and fill in real values from your
# Firebase Console → Project Settings → General → Your web app.
#
# IMPORTANT: Do NOT commit a populated `.env.local` with secrets.

# Backend base used by the frontend `apiFetch` helper (no trailing slash)
# For local backend development, set `NEXT_PUBLIC_API_BASE_URL` to your local backend.
# Example for local Railway/Express dev: http://localhost:8080
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
# Production/Preview: set NEXT_PUBLIC_API_BASE_URL to your deployed backend (Railway)
# Optional legacy env var (backwards compatibility):
NEXT_PUBLIC_API_URL=https://autoed-backend-production-7d0d.up.railway.app

# Firebase client config (replace placeholders with your Firebase project's values)
NEXT_PUBLIC_FIREBASE_API_KEY=YOUR_FIREBASE_API_KEY
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=YOUR_FIREBASE_AUTH_DOMAIN
NEXT_PUBLIC_FIREBASE_PROJECT_ID=YOUR_FIREBASE_PROJECT_ID
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=YOUR_FIREBASE_STORAGE_BUCKET
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=YOUR_FIREBASE_MESSAGING_SENDER_ID
NEXT_PUBLIC_FIREBASE_APP_ID=YOUR_FIREBASE_APP_ID
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=YOUR_FIREBASE_MEASUREMENT_ID

# Stripe publishable key (frontend)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_XXXXXXXXXXXX

# Usage:
# 1) Copy this file to .env.local in `ae-frontend-clean`:
#    PowerShell: Copy-Item .env.local.example .env.local
#    Bash: cp .env.local.example .env.local
# 2) Fill in the real values from your Firebase console and Vercel project settings.
# 3) Restart the dev server.
